package com.example.myownframework.KOTLIN_ANDROID.classes.kotlin

/*In Kotlin, inline classes are a special feature that allows you to wrap a value type without
the overhead of creating a separate object in memory. They’re useful for situations where you need
a type for clarity or additional type safety but want to avoid performance costs.*/